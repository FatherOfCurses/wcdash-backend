applications:
  - name: wcdashdatasource
    routes:
      - route:
    buildpack: java_current
    instances: 1
    memory: 1024M
    timeout: 180
    env:
      SPRING_PROFILES_ACTIVE: development
    services:
      - newrelic
      - logging
    blue-green:
      health-check-endpoint: /health

